import { putBlockIndexes } from '../src/index.js'

/** @type {Record<string, import('entail').Test>} */
export const test = {
  'should not index a block that is already indexed': async assert => {
    // await putBlockIndexes(endpoint, root)
  }
}
